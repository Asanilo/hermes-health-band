name: health-band
version: 1.1.0
description: "Read health data from HUAWEI Band 8 (heart rate, steps, SpO2, sleep, stress, battery) via Gadgetbridge sync."
config:
  retention_days:
    type: integer
    default: 30
    description: Number of days to retain historical data in derived/history/ before cleanup.
