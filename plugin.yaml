name: health-band
version: 1.1.0
description: "Read health data from Gadgetbridge-compatible smart bands (heart rate, steps, SpO2, sleep, stress, battery)."
config:
  retention_days:
    type: integer
    default: 30
    description: Number of days to retain historical data in derived/history/ before cleanup.
